# Nautilus Quant System 規則

- 核心資料同步必須獨立於 Hermes、LLM、Dashboard 與聊天平台。
- 市場歷史資料只使用 Binance USD-M 公開 API；不得要求、讀取或保存交易憑證。
- 唯一 writer 由 OS `flock` 保證；不得繞過 lock 直接改 Catalog 或 funding store。
- UTC 完整邊界、去重、連續性、精度與寫後 readback 屬 correctness，不得降級成 advisory。
- 不新增 authorization gate。審計與報表只提供 evidence，不能阻擋 technically valid research。
- 保持最小依賴；優先 stdlib 與 NautilusTrader 原生型別。
- 非平凡邏輯必須有可執行測試。修改資料模型或 persistence 後，必跑 bounded public-API smoke 與 Catalog readback。
- 對漢秦哥明確要求的修改或實作，若 repository 已設定正式 remote，完成預設包含：依風險驗證、建立乾淨 commit、推送 configured upstream，並從遠端讀回核對；不需要漢秦哥另行要求「同步 GitHub」。
- 每次 commit／push 前必須通過 `scripts/check_secrets.py` 與 configured Git hooks；不得使用 `--no-verify`。API key、token、憑證與個人資料只可存在於被忽略且權限受控的環境檔或 OS Keychain，程式只讀環境變數。
- 只有在漢秦哥明示 local-only／draft／不推送、驗證未通過、變更含敏感或實跑資料、工作區混有無關 dirty changes，或 remote／authentication 異常時，才停止推送並直報原因。`data/`、`var/`、`.local/`、`.venv/` 永不上傳。
- 實盤 execution 不在目前範圍；除非漢秦哥明確要求，不建立或啟用 live trading。
